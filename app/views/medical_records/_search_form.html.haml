= form_tag user_medical_records_path(user), method: :get do
  = hidden_field_tag :direction, params[:direction]
  = hidden_field_tag :sort, params[:sort]
  .form-inline
    = text_field_tag :search, params[:search], placeholder: 'Wpisz numer karty lub datę wizyty', class: 'form-control', size: 27
    = submit_tag "Szukaj", name: nil, class: 'btn btn-default'
    - unless current_user.pacjent?
      .pull-right
        = link_to 'Nowa karta', new_user_medical_record_path(user), class: "btn btn-default"
