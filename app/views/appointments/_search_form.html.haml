= form_tag user_appointments_path(user), method: :get do
  = hidden_field_tag :direction, params[:direction]
  = hidden_field_tag :sort, params[:sort]
  .form-inline
    = text_field_tag :search, params[:search], placeholder: 'Wpisz datę wizyty', class: 'form-control'
    = submit_tag "Szukaj", name: nil, class: 'btn btn-default'
    .pull-right
      = link_to 'Nowa wizyta', user_veterinarians_path(user), class: "btn btn-default"
