%body
  %nav.navbar.navbar-default.navbar-static-top
    .container-fluid
      .navbar-header
        %span.navbar-brand Veterinary Clinic
      %ul.nav.navbar-nav
        - if current_user.admin? || current_user.veterinarian?
          %li= link_to "Użytkownicy", users_path
      %ul.nav.navbar-nav.navbar-right
        - if user_signed_in?
          %li.navbar-text
            Logged in as #{current_user.email}
          %li= image_tag current_user.gravatar_url(size: 40)
          %li.dropdown
            %a.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :href => "#", :role => "button"}
              = current_user.full_name
            %ul.dropdown-menu
              %li= link_to "Profile", user_path(current_user)
              %li= link_to "Animals", user_animals_path(current_user)
              %li.divider{role: "separator"}
              %li= link_to "Logout", destroy_user_session_path, method: :delete
        - else
          %li= link_to "Sign up", new_user_registration_path
          %li= link_to "Sign in", new_user_session_path
